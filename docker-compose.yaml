services:
  ifa-api:
    build: .
    container_name: ifa-api
    ports:
      - "9000:80"
    volumes:
      - ./backend:/var/www/html
      - ./apache.conf:/etc/apache2/sites-enabled/000-default.conf
